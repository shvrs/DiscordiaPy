from setuptools import setup, find_packages

setup(
    name="DiscordiaPy",
    version="0.1.0",
    author="shvrs",
    description="A simple Discord API wrapper for Discord's api.",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.7",
)
